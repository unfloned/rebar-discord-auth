export const DiscordAuthConfig = {
    APPLICATION_ID: '',
    SERVER_ID: '', // leave empty if do not use whitelist | REQUIRED rebar-discord
    WHITELIST_ROLE_ID: '', // leave empty if do not use whitelist | REQUIRED rebar-discord
    SESSION_EXPIRE_TIME_IN_MINUTES: 3,
}